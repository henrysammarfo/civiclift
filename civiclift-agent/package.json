{
  "name": "civiclift-agent",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "mkdir -p /app/.openclaw/agents/main/agent && node -e \"const k=process.env.FLOCK_API_KEY; const c=JSON.stringify({'flock':{'token':k},'flock:manual':{'token':k}}); require('fs').writeFileSync('/app/.openclaw/agents/main/agent/auth-profiles.json', c); require('fs').writeFileSync('/app/.openclaw/auth-profiles.json', c); console.log('Auth provisioned');\" && openclaw gateway --allow-unconfigured",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.13.6",
    "better-sqlite3": "^12.6.2",
    "openclaw": "latest"
  }
}